spring:
  datasource:
    url: jdbc:h2:file:./db/executor;DB_CLOSE_DELAY=-1
    username: dwh
    password: ""
    driverClassName: org.h2.Driver

  h2:
    console.enabled: true

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update

server:
  port: 3308